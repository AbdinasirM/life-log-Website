import { Component } from '@angular/core';

@Component({
  selector: 'app-feedback',
  templateUrl: './feedback.component.html', // Template file for rendering UI
  styleUrls: ['./feedback.component.scss'] // Styles for the component
})
export class FeedbackComponent {
  // This component doesn't contain any specific logic, as evident from the class body.
  // It might be intended for displaying and handling user feedback in the future.
}
